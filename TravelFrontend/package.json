{
  "dependencies": {
    "sass": "^1.81.0"
  },
  "scripts": {
    "start": "live-server --port=3000"
  },
  "proxy": "http://localhost:8080",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
